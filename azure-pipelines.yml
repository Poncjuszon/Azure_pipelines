
trigger: none

pool:
  vmImage: 'windows-latest'

parameters:
  - name: environment
    displayName: Environment
    type: string
    values:
      - PROD
      - UAT
      - QA
      - DigitalHub-INT


variables:
  - template: DevOps/envs/${{ parameters.environment }}.yaml

stages:
  - stage: Publish
    displayName: Publish content of src directory to Artifactory
    jobs:
      - job: publish
        steps:
          - publish: '$(System.DefaultWorkingDirectory)/src'
            artifact: 'Apps'

  - stage: Deploy_to_environment
    displayName: 'Copy files to Apps directory'
    jobs:
      - deployment: deployment
        displayName: "Deployment"
        environment: 
          name: ${{ parameters.environment }}
          tags: ${{variables.Sitecore_Roles}}
          resourceType: virtualMachine
        strategy:
          runOnce:
            deploy:
              steps:
                - task: CopyFiles@2
                  inputs:
                    SourceFolder: '$(System.DefaultWorkingDirectory)\..\Apps\'
                    Contents: '**'
                    TargetFolder: '${{ variables.web_rootCM }}\Apps'
                    OverWrite: true
